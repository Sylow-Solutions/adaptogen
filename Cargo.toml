[package]
name = "adaptogen"
version = "0.1.0"
edition = "2021"
description = "A library c4rate for parsing different language model API responses into a normalized format"
licence = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
